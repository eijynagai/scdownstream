nextflow_process {

    name "Test Process SCANPY_LEIDEN"
    script "modules/local/scanpy/leiden/main.nf"
    process "SCANPY_LEIDEN"

        setup {
        run("SCANPY_PCA") {
            script "modules/local/scanpy/pca/main.nf"
            process {
                """
                input[0] = Channel.of([
                        [ id: 'test' ],
                        file(params.pipelines_testdata_base_path + 'lupus/samples/sarin.h5ad', checkIfExists: true)
                    ]
                )
                """
            }
        }
        run("SCANPY_NEIGHBORS") {
            script "modules/local/scanpy/neighbors/main.nf"
            process {
                """
                input[0] = SCANPY_PCA.out.h5ad
                """
            }
        }
    }

    test("Should run without failures") {

        config "./nextflow.config"

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = SCANPY_NEIGHBORS.out.h5ad.map { meta, h5ad -> [meta, h5ad, 1.0] }
                input[1] = false
                """
            }
        }

        then {
            assertAll(
            { assert process.success },
            { assert snapshot(process.out).match() }
            )
        }

    }

    test("Should run without resolution null") {

        config "./nextflow.config"

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = SCANPY_NEIGHBORS.out.h5ad.map { meta, h5ad -> [meta, h5ad, null] }
                input[1] = false
                """
            }
        }

        then {
            assertAll(
            { assert process.success },
            { assert snapshot(process.out).match() }
            )
        }

    }

    test("Should run without resolution empty list") {

        config "./nextflow.config"

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = SCANPY_NEIGHBORS.out.h5ad.map { meta, h5ad -> [meta, h5ad, []] }
                input[1] = false
                """
            }
        }

        then {
            assertAll(
            { assert process.success },
            { assert snapshot(process.out).match() }
            )
        }

    }

    test("Should run without failures - stub") {

        options '-stub'

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = SCANPY_NEIGHBORS.out.h5ad.map { meta, h5ad -> [meta, h5ad, 1.0] }
                input[1] = false
                """
            }
        }

        then {
            assertAll(
            { assert process.success },
            { assert snapshot(process.out).match() }
            )
        }

    }

}
