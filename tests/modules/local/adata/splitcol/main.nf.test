nextflow_process {

    name "Test Process ADATA_SPLITCOL"
    script "modules/local/adata/splitcol/main.nf"
    process "ADATA_SPLITCOL"

    test("Should run without failures") {

        when {
            process {
                """
                input[0] = Channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + 'pbmc/combined_filtered_matrix.h5ad', checkIfExists: true)
                ])
                input[1] = 'sample'
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

    test("Should run without failures - stub") {

        options '-stub'

        when {
            process {
                """
                input[0] = Channel.of([
                    [id: 'test'],
                    file(params.pipelines_testdata_base_path + 'pbmc/combined_filtered_matrix.h5ad', checkIfExists: true)
                ])
                input[1] = 'sample'
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
