nextflow_process {

    name "Test Process SCVITOOLS_SCVI"
    script "modules/local/scvitools/scvi/main.nf"
    process "SCVITOOLS_SCVI"

    test("Should run without failures") {

        config "./nextflow.config"

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = Channel.of([
                        [ id: 'test' ],
                        file(params.pipelines_testdata_base_path + 'samples/SAMN14430799_raw_matrix_subset_with_batch.h5ad', checkIfExists: true)
                    ]
                )
                input[1] = [[], []]
                input[2] = ''
                input[3] = ''
                """
            }
        }

        then {
            assertAll(
            { assert process.success },
            { assert snapshot(
                    process.out.h5ad,
                    process.out.obsm,
                    process.out.versions
                ).match()
            },
            )
        }

    }

    test("Should run without failures - stub") {

        options '-stub'
        config "./nextflow.config"

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = Channel.of([
                        [ id: 'test' ],
                        file(params.pipelines_testdata_base_path + 'samples/SAMN14430799_raw_matrix_subset_with_batch.h5ad', checkIfExists: true)
                    ]
                )
                input[1] = [[], []]
                input[2] = ''
                input[3] = ''
                """
            }
        }

        then {
            assertAll(
            { assert process.success },
            { assert snapshot(process.out).match() }
            )
        }

    }

}
